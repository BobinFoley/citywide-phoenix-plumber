import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Update `site` if you use a custom domain.
  site: 'https://bobinfoley.github.io',
  // `base` matches the repo name so links work on a GitHub Pages project site.
  // If you deploy at the domain root (custom domain or user/org page), set base: '/'.
  base: '/citywide-phoenix-plumber/',
});
